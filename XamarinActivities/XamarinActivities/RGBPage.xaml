<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinActivities.RGBPage">
    <ContentPage.Content>
        <StackLayout Padding="20,20,20,20" >
            <BoxView x:Name="result" VerticalOptions="Center" HeightRequest="100" />
            <Label   BindingContext="{Binding Source={x:Reference red_slider} }" Text="{Binding Value, StringFormat='Red: {0:F0}'}"/>
            <Slider ValueChanged="Slider_ValueChanged" x:Name="red_slider" BackgroundColor="#94110a" />
            <Label   BindingContext="{Binding Source={x:Reference green_slider} }" Text="{Binding Value, StringFormat='Green: {0:F0}'}"/>
            <Slider  ValueChanged="Slider_ValueChanged" x:Name="green_slider" BackgroundColor="#004a14"/>
            <Label BindingContext="{Binding Source={x:Reference blue_slider} }" Text="{Binding Value, StringFormat='Blue: {0:F0}'}"/>
            <Slider ValueChanged="Slider_ValueChanged" x:Name="blue_slider" BackgroundColor="#0c0f6e"/>
            <StackLayout VerticalOptions="EndAndExpand" >
                <Label Text="Exercise 02 @ Melrose Mejidana"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>