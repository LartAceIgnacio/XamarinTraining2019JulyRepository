<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XAML.ListExercise">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="personListView">
                <ListView.Header>
                    <Label>My Contacts</Label>
                </ListView.Header>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <AbsoluteLayout Padding="10">
                                    <BoxView BackgroundColor="Pink"/>
                                    <Label Text="{Binding NameInitial}"
                                           FontSize="12"/>
                                </AbsoluteLayout>
                                    <Label Text="{Binding FullDetails}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>