<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProjectDemo.ContactPage">
    <ContentPage.Content>
		<StackLayout>
			<SearchBar Placeholder="Search" TextChanged="SearchBar_TextChanged"/>
			<ListView x:Name="contactListView"
					  SeparatorColor="#707070"
					  IsPullToRefreshEnabled="True"
					  Refreshing="ContactListView_Refreshing">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ImageCell ImageSource="https://coolbackgrounds.io/images/backgrounds/black/pure-black-background-f9eeb41f.jpg"
								   Text="{Binding FullName}"
								   Detail="{Binding PhoneNumber}">
								<ImageCell.ContextActions>
									<MenuItem Clicked="Delete_Clicked" CommandParameter="{Binding .}" Text="Delete"/>
								</ImageCell.ContextActions>
								</ImageCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>